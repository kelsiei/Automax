namespace CarCareTracker.Enum;

public enum ExtraFieldType
{
    Text = 0,
    Number = 1,
    Date = 2,
    Checkbox = 3
    // TODO: include additional types (e.g., Select, MultiSelect) when UI supports them.
}
